<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="styles/ultimate.css">
    <title>Saved Cars</title>
</head>

<body>
    <%- include('partials/header') %>
    <section id="allVehicles">
        <% for(let i = 0; i < vehicles.length; i++) { %>
        <a id="cards" href="<%= vehicles[i].url %>">
            <h2><%= vehicles[i].title %></h2>
            <h3><%= vehicles[i].price %></h3>
            <h3><%= vehicles[i].market_value %></h3>
            <img class="carImages" src="<%= vehicles[i].image_url %>" alt="image">
            <form action="/savedCars/<%= vehicles[i].id %>?_method=DELETE" method="POST">
                <input type="submit" name="remove" value="REMOVE" id="">
            </form>
            <a href="/map?lat=<%= vehicles[i].lat %>&long=<%= vehicles[i].long %>">
                Lookup Map</a>
            <% } %>
        </a>
    </section>
</body>

</html>